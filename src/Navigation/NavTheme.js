import { DefaultTheme } from "@react-navigation/native";

export default NavTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#481acb",
    background: "#fff",
  },
};
